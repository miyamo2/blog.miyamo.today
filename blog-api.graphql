query GetAllTags {
  tags {
    edges {
      cursor
      node {
        id
        name
        articles {
          edges {
            cursor
          }
          totalCount
        }
      }
    }
  }
}
